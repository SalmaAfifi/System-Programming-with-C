#include<stdio.h>
#include <string.h>

int main(int argc, char *argv[]){
    int len = strlen(argv[argc-1]);
    printf("%d\n",len);

}

//Write a C program, which prints out 
//the length of its last command line argument.
